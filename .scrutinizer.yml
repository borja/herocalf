build:
  environment:
    ruby: '2.6.5'
  nodes:
    analysis:
      tests:
        override:
          - ruby-scrutinizer-run
    tests-with-coverage:
      tests:
        override:
          - command: bundle exec rspec test.rb
            environment:
              'SCRUTINIZER_CC_FILE': 'my-coverage'
            coverage:
              file: 'my-coverage'
              format: 'rb-cc'
filter:
  excluded_paths:
    - coverage/*
    - data/*
    - public/*