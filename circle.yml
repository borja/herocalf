machine:
  ruby:
    version: 2.5.0
test:
  pre:
    - ./cc-test-reporter before-build
  override:
    - bundle show yaml-lint
    - $(bundle show yaml-lint)/bin/yaml-lint data/
    - bundle exec rspec test.rb; ./cc-test-reporter after-build --exit-code $?
