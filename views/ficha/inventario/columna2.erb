<%= erb :'ficha/inventario/anillos' %>
<div class="table-responsive">
	<table class="table">
		<thead>
			<tr><th colspan=2 class="text-center">Atuendos</th></tr>
			<tr>
				<% fields[1].each do |field| %>
					<%= erb :'ficha/inventario/field', locals: { fits: field } %>
				<% end %>
			</tr>
		</thead>
	</table>
</div>