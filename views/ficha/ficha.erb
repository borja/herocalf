<% h = hero(params[:param].to_i) %>

<div class="row">
	<div class="col-lg-12">
		<%= erb :'ficha/nav/tabs' %>
		<div class="tab-content" id="myTabContent">

			<div class="tab-pane fade show active" id="stats" role="tabpanel" aria-labelledby="stats-tab">
				<div class="row">
					<%= erb :'ficha/nav/stats' %>
				</div>
			</div>

			<div class="tab-pane fade" id="lore" role="tabpanel" aria-labelledby="lore-tab">
				<div class="row">
					<%= erb :'ficha/nav/lore' %>
				</div>
			</div>

			<div class="tab-pane fade" id="inv" role="tabpanel" aria-labelledby="inv-tab">
				<div class="row">
					<%= erb :'ficha/nav/inv' %>
				</div>
			</div>

			<div class="tab-pane fade" id="habs" role="tabpanel" aria-labelledby="habs-tab">
				<div class="row">
					<%= erb :'ficha/nav/habs' %>
				</div>
			</div>

			<div class="tab-pane fade" id="magia" role="tabpanel" aria-labelledby="magia-tab">
				<div class="row">
					<%= erb :'ficha/nav/magia' %>
				</div>
			</div>
		</div>
	</div>
</div>
