<div class="row">
		<div class="col-lg-12">
				<h1 class="page-header">
					<%= breadcrumb(params[:view])[:title].capitalize %>
				</h1>
				<ol class="breadcrumb">
						<li>
							<a href="/<%= breadcrumb(params[:view])[:bc].first.capitalize %>">
								<%= breadcrumb(params[:view])[:bc].first.capitalize %>
							</a>
						</li>
						<li class="active"><%= params[:view].capitalize %></li>
				</ol>
		</div>
</div>