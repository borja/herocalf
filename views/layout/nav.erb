<%= erb :'layout/nav/navbar-header', locals: { title: title } %>

<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

	<!-- Main website topics -->
	<ul class="nav navbar-nav">
		<% %w(heroes objetos magia manuales ciudad habilidades).each do |topic| %>
			<li class="dropdown"><%= erb :"layout/nav/#{topic}" %></li>
		<% end %>
	</ul>

	<!-- User contents -->
	<%= erb :'layout/nav/usercontent' %>
</div>